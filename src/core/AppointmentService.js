import { APPOINTMENTS } from './urls'

export default class AppointmentService {
    constructor($http) {
        this.$http = $http;
    }

    getAllAppointments(key) {
        return undefined;
    }
}
